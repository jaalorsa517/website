# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy website
"on":
  push:
    branches:
      - master
jobs:
  build:
    runs-on: ubuntu-latest
    environment:
      name: website
    steps:
      - name: Set enviroments
        run: |
          echo 'VITE_YOUTUBE_API_HOST=${{vars.VITE_YOUTUBE_API_HOST}}' >> $GITHUB_ENV
          echo 'VITE_YOUTUBE_LIST=${{vars.VITE_YOUTUBE_LIST}}' >> $GITHUB_ENV
          echo 'VITE_FIREBASE_APIKEY=${{secrets.VITE_FIREBASE_APIKEY}}' >> $GITHUB_ENV
          echo 'VITE_FIREBASE_AUTH_DOMAIN=${{vars.VITE_FIREBASE_AUTH_DOMAIN}}' >> $GITHUB_ENV
          echo 'VITE_FIREBASE_PROJECT_ID=${{vars.VITE_FIREBASE_PROJECT_ID}}' >> $GITHUB_ENV
          echo 'VITE_FIREBASE_STORAGE_BUCKET=${{vars.VITE_FIREBASE_STORAGE_BUCKET}}' >> $GITHUB_ENV
          echo 'VITE_FIREBASE_MESSAGING_SENDER_ID=${{vars.VITE_FIREBASE_MESSAGING_SENDER_ID}}' >> $GITHUB_ENV
          echo 'VITE_FIREBASE_APP_ID=${{vars.VITE_FIREBASE_APP_ID}}' >> $GITHUB_ENV
          echo 'VITE_FIREBASE_MEASUREMENT_ID=${{vars.VITE_FIREBASE_MEASUREMENT_ID}}' >> $GITHUB_ENV

      - name: Use Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "18.x"

      - name: Checkout
        uses: actions/checkout@v4

      - name: Install dependencies
        run: npm ci

      - name: Build project
        run: npm run build

      - name: Upload artifact
        uses: actions/upload-artifact@v3
        with:
          name: build-website
          path: dist
          retention-days: 1

          
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: website
    needs: [build]
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Download artifact
        uses: actions/download-artifact@v3
        with:
          name: build-website
          path: dist
          
      - name: Deploy to Firebase
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: "${{ secrets.GITHUB_TOKEN }}"
          firebaseServiceAccount: "${{ secrets.FIREBASE_SERVICE_ACCOUNT_WEBPAGE_7E920 }}"
          channelId: live
          projectId: webpage-7e920
          target: website

  release:
    runs-on: ubuntu-latest
    needs: [deploy]
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Get version from package.json
        id: get_version
        run: echo "version=$(node -p -e "require('./package.json').version")" >> $GITHUB_OUTPUT
      
      - name: Create Release
        id: create_release
        uses: softprops/action-gh-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: v${{ steps.get_version.outputs.version }}
          name: Release ${{ steps.get_version.outputs.version }}